# Demo 启动入口

双击项目根目录的 `启动Demo.cmd`。

入口会自动：

1. 以本地 Demo 模式启动前端；
2. 使用“原型评审账号”进入系统，不跳转 SSO；
3. 打开 `http://127.0.0.1:8080/dashboard`；
4. 关闭启动窗口后停止 Demo。

Demo 模式仅用于本地原型评审，不影响 dev、staging 或 production 的 SSO 和接口行为。
